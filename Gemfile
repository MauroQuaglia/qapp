source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'rails'
gem 'webrick'
gem 'sass-rails'
gem 'uglifier'

=begin
group(:development) do
  gem 'thin'
end
=end
