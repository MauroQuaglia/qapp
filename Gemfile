source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem 'rails'
gem 'webrick'
gem 'sass-rails'
gem 'uglifier'

group(:test) do
  gem('rspec-rails')
  gem('capybara')
end

=begin
group(:development) do
  gem 'thin'
end
=end
